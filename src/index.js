import React from "react";
import ReactDOM from "react-dom";

import PasswordReset from "./password-reset/password-reset-2";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <PasswordReset />
  </React.StrictMode>,
  rootElement
);
